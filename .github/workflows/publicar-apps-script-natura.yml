name: Google Apps Script es la fuente principal

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  protegido:
    runs-on: ubuntu-latest
    steps:
      - name: Mantener Google como fuente principal
        run: |
          echo "Google Apps Script es la fuente principal."
          echo "Este workflow no publica, no despliega y no elimina nada en Google."
          echo "La sincronización permitida es Google Apps Script -> GitHub."
