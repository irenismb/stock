 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/natura/catalogo.html b/natura/catalogo.html
index 1c6637049f043d79ec88f1e47ba8fd8f19992453..ac98d4324b9157af94ffda204fd6dc214dabc0bc 100644
--- a/natura/catalogo.html
+++ b/natura/catalogo.html
@@ -31,52 +31,52 @@
   <meta name="twitter:description" content="Catálogo Natura en Santa Marta. Compra fácil por WhatsApp." />
   <meta name="twitter:image" content="https://irenismb.github.io/stock/natura/recursos/otras_imagenes/logo_empresa.png" />
 
   <link rel="dns-prefetch" href="//web.whatsapp.com" />
   <link rel="preconnect" href="https://web.whatsapp.com" crossorigin />
   <link rel="preconnect" href="https://api.github.com" crossorigin />
   <link rel="preload" as="image" href="recursos/otras_imagenes/logo_empresa.png" fetchpriority="high" />
 
   <style>
     :root{
       --bg:#0b0f14;
       --card:#121826;
       --txt:#e7edf5;
       --mut:#a9b4c2;
       --acc:#20c997;
       --br:14px;
       --line:#22304a;
       --focus:#60a5fa;
       --pill:#0e1524;
       --danger:#ef4444;
 
       --social-size: clamp(26px, 8.5vw, 38px);
       --social-gap: clamp(3px, 1.6vw, 10px);
 
       /* ✅ NUEVO: colores llamativos para el contador cuando hay búsqueda */
-      --count-blink:#facc15; /* amarillo fuerte */
-      --count-blink-2:#fb7185; /* rosa coral */
+      --count-blink:#ffea00; /* amarillo neón */
+      --count-blink-2:#ff2d95; /* fucsia vibrante */
     }
 
     *{box-sizing:border-box}
     html,body{max-width:100%;overflow-x:hidden}
     body{
       margin:0;
       font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial;
       background:linear-gradient(180deg,#0b0f14,#0c1220);
       color:var(--txt);
     }
     a{color:inherit}
     .wrap{max-width:1100px;margin:0 auto}
 
     .site-header{ padding:16px 16px 10px; }
     .site-header-inner{
       display:flex;
       align-items:center;
       justify-content:space-between;
       gap:14px;
       flex-wrap:wrap;
       border:1px solid var(--line);
       background:rgba(18,24,38,.55);
       border-radius:18px;
       padding:12px 14px;
     }
@@ -289,106 +289,106 @@
     .topline.hidden{ display:none; } /* ✅ NUEVO: ocultar el topline cuando el contador se mueve a móvil */
 
     /* ✅ Mejoras para el contador (incluye modo parpadeo) */
     .count{
       font-size:12px;
       color:var(--mut);
       display:inline-block;
       padding:6px 10px;
       border-radius:999px;
       border:1px solid transparent;
       background:rgba(11,18,32,.18);
       max-width:100%;
     }
 
     /* ✅ NUEVO: slot para el contador justo debajo de la barra de búsqueda en móvil */
     .count-slot{
       display:none;         /* solo móvil */
       width:100%;
     }
 
     @keyframes countBlink{
       0%, 100%{
         opacity:1;
         transform:translateY(0);
         text-shadow:
-          0 0 10px rgba(250,204,21,.55),
-          0 0 22px rgba(251,113,133,.35);
-        border-color: rgba(250,204,21,.35);
+          0 0 10px rgba(255,234,0,.65),
+          0 0 22px rgba(255,45,149,.45);
+        border-color: rgba(255,234,0,.45);
       }
       50%{
         opacity:.28;
         transform:translateY(-.5px);
         text-shadow:
-          0 0 2px rgba(250,204,21,.22),
-          0 0 8px rgba(251,113,133,.22);
-        border-color: rgba(251,113,133,.25);
+          0 0 2px rgba(255,234,0,.30),
+          0 0 8px rgba(255,45,149,.30);
+        border-color: rgba(255,45,149,.35);
       }
     }
     .count.search-active{
       color:var(--count-blink);
       font-weight:950;
-      background:linear-gradient(90deg, rgba(250,204,21,.14), rgba(251,113,133,.10));
-      animation: countBlink 1.15s ease-in-out infinite; /* < 3 parpadeos/seg */
+      background:linear-gradient(90deg, rgba(255,234,0,.18), rgba(255,45,149,.14));
+      animation: countBlink .85s ease-in-out infinite; /* parpadeo más notorio */
     }
     @media (prefers-reduced-motion: reduce){
       .count.search-active{
         animation:none;
         opacity:1;
       }
     }
 
     .grid{
       display:grid;
       gap:12px;
       margin-top:12px;
       grid-template-columns:repeat(4, minmax(0, 1fr));
     }
     @media (max-width:1024px){
       .grid{ grid-template-columns:repeat(3, minmax(0, 1fr)); }
     }
     @media (max-width:760px){
       .grid{ grid-template-columns:repeat(2, minmax(0, 1fr)); }
     }
     @media (max-width:420px){
       .grid{ grid-template-columns:repeat(1, minmax(0, 1fr)); }
     }
 
     .card{
       background:rgba(18,24,38,.92);
       border:1px solid var(--line);
       border-radius:var(--br);
       overflow:hidden;
       display:flex;
       flex-direction:column;
       min-height:100%;
 
       content-visibility:auto;
       contain-intrinsic-size: 420px 520px;
     }
     .img{
-      aspect-ratio:4/3;
+      aspect-ratio:1/1;
       background:#0b1220;
       display:flex;align-items:center;justify-content:center;
       border-bottom:1px solid var(--line);
     }
     .img img{
       width:100%;
       height:100%;
       object-fit:cover;
       cursor:zoom-in;
     }
 
     .pad{padding:12px;display:flex;flex-direction:column;gap:10px}
     .name{font-weight:950;margin:0;font-size:15px;line-height:1.25}
     .meta{color:var(--mut);font-size:12px;margin:0}
     .row{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap}
     .price{font-weight:950}
 
     .pill{
       display:inline-block;font-size:11px;padding:4px 8px;border-radius:999px;
       border:1px solid #2a3b5c;color:var(--mut);
       background:rgba(11,18,32,.35);
       white-space:nowrap;
     }
 
     #cartCount{
@@ -850,51 +850,51 @@
         <div class="cart-total" id="cartTotal">Total: $0</div>
         <div class="cart-footer-actions">
           <button class="btn-acc" id="cartBuyBtn" type="button">Comprar por WhatsApp</button>
         </div>
       </div>
     </div>
   </div>
 
   <div class="img-modal" id="imgModal" aria-hidden="true" role="dialog" aria-label="Imagen ampliada">
     <div class="img-modal-backdrop" id="imgModalBackdrop"></div>
     <button class="img-modal-close" id="imgModalClose" type="button" aria-label="Cerrar">✕</button>
     <div class="img-modal-content" role="document">
       <img id="imgModalImg" alt="Imagen ampliada del producto" />
     </div>
   </div>
 
   <script>
     /* ==========================
        ✅ OPTIMIZACIONES CLAVE
        - 1 sola llamada rápida a GitHub (Contents API) en vez de tree recursivo
        - Cache con "stale-while-revalidate": pinta rápido con cache y actualiza en segundo plano
        - Debounce en búsqueda para evitar renders excesivos
        - Render por bloques (chunked) para no congelar el móvil
        - JSON-LD y ticker se calculan en idle para mejorar percepción de carga
        - ✅ NUEVO: en móvil el contador se mueve debajo del buscador y centrado
-       - ✅ NUEVO: si hay resultados con filtros/búsqueda, muestra "Los filtré abajo ⬇️"
+       - ✅ Muestra contador con formato: N producto(s) encontrado(s).
        ========================== */
 
     const APP_FLAGS = {
       SHOW_STOCK_QUANTITY: false,
       SHOW_STOCK_STATUS_TEXT: false,
       ENABLE_GPS_LOCATION: true,
 
       ENFORCE_STOCK_LIMITS: false
     };
     window.APP_FLAGS = APP_FLAGS;
 
     function shouldEnforceStockLimits(){
       return !!(window.APP_FLAGS && window.APP_FLAGS.ENFORCE_STOCK_LIMITS === true);
     }
 
     // Utilidad idle
     const _hasIdle = ("requestIdleCallback" in window);
     function runIdle(fn, timeout=1200){
       if(_hasIdle) return requestIdleCallback(fn, { timeout });
       return setTimeout(fn, Math.min(250, timeout));
     }
 
     const WHATSAPP_NUMBER = "573042088961";
 
     const IMG_DIR = "recursos/imagenes_de_productos";
@@ -1856,64 +1856,64 @@
         if(terms.length){
           return terms.every(t => p.searchKey.includes(t));
         }
         return true;
       });
 
       if(sortMode === "price_asc"){
         filtered.sort((a,b)=> (a.price||0) - (b.price||0) || String(a.id).localeCompare(String(b.id)));
       }else if(sortMode === "price_desc"){
         filtered.sort((a,b)=> (b.price||0) - (a.price||0) || String(a.id).localeCompare(String(b.id)));
       }else if(sortMode === "name_asc"){
         filtered.sort((a,b)=>{
           const cmp = String(a.name||"").localeCompare(String(b.name||""), "es", { sensitivity:"base" });
           if(cmp !== 0) return cmp;
           return String(a.id).localeCompare(String(b.id));
         });
       }
       return filtered;
     }
 
     let _renderToken = 0;
     function render(){
       const token = ++_renderToken;
       const filtered = buildFilteredList();
 
-      /* ✅ NUEVO: mensaje del contador centrado (y con “Los filtré abajo ⬇️” si aplica) */
+      /* ✅ Mensaje de resultados con formato solicitado */
       const cat = catSel.value;
       const br = brandSel.value;
       const sortMode = sortSel ? sortSel.value : "";
       const qTxt = String(qInp.value || "").trim();
       const hasFilters = !!qTxt || !!cat || !!br || !!sortMode;
 
       if(filtered.length === 0){
         countEl.textContent = hasFilters
-          ? "0 producto(s) encontrado(s). No encontré coincidencias; prueba con otra búsqueda."
+          ? "0 producto(s) encontrado(s)."
           : "0 producto(s) disponible(s).";
       }else{
         countEl.textContent = hasFilters
-          ? `Se encontraron ${filtered.length} producto(s). Los filtré abajo ⬇️`
+          ? `${filtered.length} producto(s) encontrado(s).`
           : `Hay ${filtered.length} producto(s) disponible(s).`;
       }
 
       updateCountAttention();
       placeCountElement(); /* por si cambia orientación o el DOM */
 
       grid.textContent = "";
 
       const CHUNK = 60;
       let i = 0;
 
       function drawChunk(){
         if(token !== _renderToken) return;
 
         const frag = document.createDocumentFragment();
         const end = Math.min(i + CHUNK, filtered.length);
         for(; i < end; i++){
           frag.appendChild(makeCard(filtered[i]));
         }
         grid.appendChild(frag);
 
         if(i < filtered.length){
           requestAnimationFrame(drawChunk);
         }
       }
 
EOF
)